---
name: Scheduled Test Workflow
on:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "This current server time is $(date)"



